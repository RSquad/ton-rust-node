{{- if and .Values.zerostate (not .Values.existingZerostateConfigMapName) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "node.fullname" . }}-zerostate
  labels:
    {{- include "node.labels" . | nindent 4 }}
binaryData:
  zerostate.boc: {{ .Values.zerostate }}
{{- end }}
