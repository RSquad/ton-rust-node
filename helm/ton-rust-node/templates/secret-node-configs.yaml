{{- if and .Values.nodeConfigs (not .Values.existingNodeConfigsSecretName) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "node.fullname" . }}-node-configs
  labels:
    {{- include "node.labels" . | nindent 4 }}
stringData:
  {{- range $key, $value := .Values.nodeConfigs }}
  {{ $key }}: |
    {{- tpl $value $ | nindent 4 }}
  {{- end }}
{{- end }}
